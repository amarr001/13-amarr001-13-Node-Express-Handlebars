<li>
	{{burger_name}}

	<button class="change-data btn btn-success" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Devour{{else}}Add to the list{{/if}}
	</button>

	<button class="delete-burger btn btn-light" data-id="{{this.id}}">DELETE!</button>
	
</li>