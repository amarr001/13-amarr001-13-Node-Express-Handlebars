<li>
	{{burger_name}}

	<button class="change-data" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Devour{{else}}Add to the list{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
	
</li>